export const environment = {
  production: false,
  // Use empty SERVER_API_URL during dev so requests go to relative /api and can be proxied by Angular dev server
  SERVER_API_URL: 'https://3aa822937d98.ngrok-free.app'
};
